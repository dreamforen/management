# 드림포이엔 시료채취기록지 웹 v5

- 먼지 / 중금속 기록지를 완전히 독립 상태로 분리
- 24시간제 시간 입력(HH:MM)
- 원형 굴뚝: 단면적 기준 1/4/8/12/16/20점 자동 산정
- 원형 측정점 위치: 등단면적 반경식 적용, 관벽 2.5 cm 최소 이격 반영
- 사각형 굴뚝: 상등직경 계산, 면적·종횡비 기반 격자 자동 분할
- 계산된 측정점 수만큼 위치표 및 입자상 측정조건 입력행 자동 생성
- 표준산소 입력 시 보정 전/후 유량 자동 표시
- 보정 전/후 농도 입력란 제거
- 팀별 오리피스계수 및 노즐 선택, K-factor·오리피스압차 자동계산
- 적산유량계 전/후 및 누출검사
- 가스상 측정조건 항목 선택 추가

GitHub Pages에서는 index.html / style.css / app.js / README.md 4개 파일을 기존 저장소에 덮어쓰면 됩니다.


## v7 변경사항
- 원형/사각형 전체 법정 측정점과 대표 측정구 입력 지점을 분리
- 원형: 대표 측정구는 반경 구분수에 따라 1~5지점만 표시 (예: D=0.78 m → 0.276 m)
- 사각형: 전체 격자수는 내부 산정, 대표 측정구는 가로 방향 중심거리 1~5지점만 표시
- 담당자 서명 박스 제거, 이름 옆 `(인)` 표시
- 먼지 시료채취시간 입력란을 제목 바로 옆으로 정렬


## v10 변경사항
- 원형 대표 측정점 로직 유지
- 사각형 대표 측정구 위치를 격자 중심 좌표로 변경 (예: 0.80×0.80 m, 2×2 등분 → 1지점 0.200×0.200 m)
- 원형/사각형 측정점을 SVG 단면 그림에 자동 표시


## v10 사각형 측정점 보정
- 단면적별 최대 L(0.5 / 0.667 / 1.0 m) 적용
- 가로·세로 각각 ceil(길이/L)로 최소 분할 수 산정, 0.25 m² 초과 시 최소 4등분
- 전체 격자와 현장 대표 측정구의 실제 입력 지점을 분리
- 0.80 × 0.80 m 입력 시 2×2 등분, 대표 1지점 0.200 × 0.200 m
- 계산 결과를 사각형/원형 도식에 자동 표시


## v10 측정구 표시
- 원형/사각형 모두 오른쪽 측부를 측정구로 고정
- 단면 그림 오른쪽에 측정구와 프로브 삽입방향 자동 표시
- 사각형 측정점 그림은 오른쪽 측정구 기준으로 좌우 방향을 맞춰 표시
- 기존 계산값 및 원형/사각형 측정점 산정 로직은 유지


## v11 산출근거(로우데이터) 인쇄
- 기록지 뒤에 산출근거 페이지 추가
- 수분량, N2, 표준/실제 밀도, 유속, Vic, 노즐면적, 굴뚝면적, 오리피스압차, 등속흡입계수, Qa, 산소보정 Q 표시
- 각 항목의 기호, 계산식, 대입값, 산출값, 단위를 함께 출력
- 지점별 원시 측정값 표 추가
- 인쇄/PDF 시 산출근거 페이지는 새 페이지에서 자동 시작
- 먼지/중금속 전환 시 각각의 독립 입력값으로 재계산


## v12 장비 선택 개선
- 1팀 노즐: 0.317 / 0.472 / 0.609 / 0.759 / 0.957 / 1.088 / 1.263 cm
- 2팀 노즐: 0.312 / 0.450 / 0.533 / 0.612 / 0.777 / 0.938 / 1.094 / 1.267 cm
- 노즐 기타 직접입력 추가
- 기타 입력 시 preset 버튼 선택 자동 해제, 계산에는 직접입력값 사용
- K-factor 옆에 등속흡인계수 즉시 표시
- 장비 선택 영역 압축 및 작은 모니터용 반응형 배치 개선


## v13 노즐 UI 수정
- 작은 모니터에서 기타 직접입력란이 노즐 버튼과 겹치던 문제 수정
- 노즐 버튼은 4열 고정 정렬
- 기타 노즐 직접입력은 별도 줄로 분리
- 매우 좁은 화면에서는 2열로 자동 전환


## v14 수정
- 오리피스계수를 측정 장비 패널에서 제거하고 피토관계수 바로 옆으로 이동
- 1팀/2팀 버튼 클릭 이벤트 누락 복구
- 팀 선택 시 오리피스계수와 노즐 목록이 즉시 변경되도록 정상화


## v15
- 피토관계수 옆 오리피스계수 칸을 표 형태로 정돈
- 계수값과 고정값 안내 문구가 겹치지 않도록 분리


## v16
- 누락된 원형/사각형 입력 전환 함수 복구
- 원형 선택 시 내경만 활성화, 가로/세로 자동 비움·비활성화
- 사각형 선택 시 가로/세로만 활성화, 내경 자동 비움·비활성화
- 단면 전환 즉시 측정점/시료채취 행 재계산


## v17 현장 기록관리
- 하루 여러 측정건을 개별 기록으로 저장
- 오늘 작성 기록 목록에서 클릭하여 다시 열기/수정
- 입력 중 0.7초 후 브라우저 자동저장
- 최초 수동저장 전에도 작성중 초안을 자동 보존하고 다음 접속 시 복구
- 기존 기록 수정 후 '기록 저장' 시 기존 건 업데이트
- 수동 저장 직전 확정본 1개 자동 백업 및 '최근 저장본 복구'
- '복사해서 새 기록'으로 업체/시설 조건을 재활용 가능
- 현재폼 초기화는 저장 기록을 삭제하지 않음
- 기록은 현재 사용하는 브라우저/기기 내부(localStorage)에 저장


## v18
- 새 기록 → 시설추가
- 복사해서 새 기록 버튼 삭제
- 시설추가 시 업체명/날짜/담당자/기상/장비정보 유지, 시설별 입력값 초기화
- 오늘 작성 기록: 접수번호 / 업체명 / 시설명 순
- Excel 출력: 인쇄 가능한 시료채취기록지 양식 + 산출근거(로우데이터) 시트
- A4 가로, 1페이지 너비 맞춤, 병합셀/테두리/구획 적용


## v19
- Excel 불러오기 추가: v18 이후 웹에서 출력한 시료채취기록지 Excel을 다시 업로드하여 자동복원
- 일반사항 제목 제거, 이후 섹션 번호 재정리
- 브라우저/PDF 및 Excel 인쇄방향 A4 세로로 변경
- 산출근거 수식을 분수형 표시로 개선해 분자/분모 구분
- 불러온 Excel은 웹에서 수정 후 기존 기록관리 기능으로 다시 저장 가능


## v20
- 첫 기본정보 카드 제목을 '현장측정정보'로 지정
- Excel 불러오기 엔진을 ExcelJS에서 SheetJS(XLSX)로 교체하여 출력본 재업로드 안정성 개선
- 시트 위치/행 위치가 조금 변해도 항목명과 헤더를 검색해 입자상/가스상 데이터를 복원
- 인쇄 레이아웃 최종 재정리: 현장측정정보~자동계산결과 1페이지, 산출근거 2페이지
- A4 세로, 첫 페이지는 약 58% 축소, 로우데이터는 큰 글씨로 별도 페이지 사용


## v21
- Excel 출력 첫 시트를 현재 웹 시료채취기록지 흐름에 맞춰 전면 재배치
- A4 세로 1페이지에 현장측정정보~자동계산결과가 들어가도록 FitToPage 적용
- 담당자(부) manager2를 Excel 출력에 추가
- 굴뚝/장비조건 아래 측정점 위치 표시 영역을 Excel에 포함
- 원형: 대표 측정점 거리, 사각형: 대표점 좌표 및 격자 표현
- 산출근거(로우데이터)는 별도 두 번째 시트/1페이지로 인쇄 설정


## v21.1 hotfix
- Excel 출력 버튼이 반응하지 않던 원인 수정
- 측정점 그림 영역에서 전체 셀 병합 후 내부 셀을 다시 병합해 ExcelJS가 오류를 내던 구조 제거
- Excel 출력 오류 발생 시 사용자에게 오류 메시지가 보이도록 예외 처리 추가


## v22
- 업로드된 기존 DFEN-QPF-14-01 인쇄양식을 기준으로 Excel 첫 시트 레이아웃 재구성
- 상단 접수번호/업체/시설/담당자/책임기술자 + 현장측정정보 + 입자상 + 가스상 순으로 A4 세로 1페이지
- 웹의 측정점 SVG를 PNG로 변환해 Excel에 그대로 이미지 삽입
- 원형/사각형/측정구/점 위치가 웹 화면과 동일한 그림으로 출력
- 가스상 측정조건은 사용자가 추가한 항목 수만큼 행 자동 증가
- 숨김 __WEB_DATA 시트에 전체 웹 입력값을 저장하여 Excel 재업로드 시 정확히 복원
- 산출근거는 별도 2번째 시트/1페이지 유지

## v23 인쇄/PDF 최종 레이아웃
- 일반 웹 화면을 축소 인쇄하지 않고 별도 인쇄 전용 문서 생성
- A4 세로, 상하좌우 10mm 여백
- 1페이지: 기존 DFEN-QPF-14-01 스타일의 현장측정정보 + 측정점 그림 + 입자상 + 가스상
- 측정점 그림은 현재 웹 SVG를 그대로 복제하여 원형/사각형/측정구/지점이 동일하게 출력
- 가스상 조건은 실제 추가한 항목 수만큼만 행 생성
- 2페이지: 수분량/밀도/유속/채취수분/등속흡입계수/유량/산소보정 계산을 큰 분수식과 변수값으로 배치
- Excel 기능은 유지하되 공식 출력물은 인쇄/PDF 중심

## v24 인쇄/PDF 개선
- 1페이지 강제 압축 제거
- A4 세로 기준 상하 10mm / 좌우 9mm 여백
- 표 전체를 인쇄 가능 가로폭 100%에 맞추고 세로는 자연스럽게 흐르도록 변경
- 제목을 표 밖 독립 중앙 제목으로 이동해 세로 깨짐 방지
- 현장측정정보 3열 비율 유지, 측정점 SVG 그대로 출력
- 입자상 표는 폭 우선으로 유지
- 가스상 조건은 추가된 행만 표시, 많으면 자연스럽게 다음 페이지로 넘어감
- 산출근거는 2페이지 이후 큰 글자/큰 수식으로 출력


## v25
- 2페이지 로우데이터는 v24 그대로 유지
- 1페이지 오른쪽 잘림 방지를 위해 A4 안전 여백을 좌우 11mm로 조정
- 상단/현장정보/입자상/가스상 각 표의 열 비율을 100% 이내로 명시
- 셀 패딩을 안전하게 줄이고 1페이지 글씨는 오히려 소폭 확대
- 측정점 그림은 화면 SVG를 단순 복제하지 않고 흰 배경 인쇄전용 SVG로 재생성
- 측정점 그림의 검은 사각형 출력 문제 보정


## v26 - 웹 입력화면 세로축 재설계
- 기능/계산/세팅값/팀별 장비값/노즐값/오리피스계수/K-factor/측정점 계산 로직 변경 없음
- 저장/기록관리/Excel 불러오기/가스상 추가/자동계산 로직 변경 없음
- 기존 HTML ID와 JavaScript 로직을 그대로 보존하고 CSS 배치만 변경
- 웹 본문 최대폭 980px의 A4 세로 문서형 화면으로 정렬
- 기존 가로 다단 카드 배치를 기본 1열 위→아래 흐름으로 변경
- 카드 내부의 짧은 입력항목만 2열 유지, 작은 화면에서는 1열
- 측정점 그림을 중앙 넓은 영역에 배치
- 2페이지 로우데이터 인쇄 레이아웃은 변경 없음


## v27
- 상단 헤더 폭과 본문 폭을 980px 동일 축으로 정렬
- 오늘 작성한 기록을 오른쪽 세로 박스가 아니라 전체폭 가로 영역으로 표시하도록 보정
- 입자상 측정조건은 가로 스크롤 없이 한 화면에서 보이도록 표 폭/글자/패딩 조정
- 인쇄/PDF는 별도 printDocument 양식을 사용하지 않고 현재 웹 화면 레이아웃을 그대로 출력하도록 변경
- A4 세로 인쇄 시 현재 화면 전체를 비율 축소하여 폭에 맞춤
- 계산/세팅값/팀 장비값/측정점/저장 데이터 로직은 변경하지 않음


## v28
- 상단 '대기 시료채취기록지' 헤더 폭을 본문 980px과 정확히 동일하게 맞춤
- 오늘 작성한 기록을 먼지/중금속 탭 오른쪽으로 이동, 기록카드를 가로로 스크롤/열람
- 피토관계수(C)와 오리피스계수를 동일 행의 강조 박스로 정리
- 입자상 12열 표의 열별 폭을 재배분하여 가로 스크롤 없이 전체 표시
- 산출근거는 No/기호/단위 폭을 줄이고 산출식/대입값 폭을 확보하여 한 화면 표시
- 지점별 원시 측정값 10열도 열별 폭 재배분하여 한 화면 표시
- 인쇄는 현재 화면 구조를 기준으로 A4 폭에 맞춰 출력
- 계산/저장/팀별 장비/측정점 로직은 변경하지 않음


## v29
- 오늘 작성한 기록을 기록저장/시설추가 등 액션 버튼 아래로 이동
- 기록 목록을 가로 카드가 아니라 세로 목록으로 변경
- A4 세로 인쇄에서 한 페이지 강제 없음
- 현재 웹 세로형 화면 구조를 그대로 유지하면서 A4 폭에 맞게 자연스럽게 여러 페이지 출력
- 표/입력칸/측정점 영역은 인쇄 시 자동 축소 및 폭 맞춤
- 계산/세팅/저장/측정점 로직은 변경하지 않음


## v30
- 오늘 작성한 기록을 액션버튼 바로 아래 왼쪽 영역(약 72%)에 세로목록으로 배치
- 인쇄 첫 페이지 상단에만 '드림포이엔 시료채취기록지' 제목 표시
- @page margin 0 + 내부 여백 방식으로 브라우저 날짜/시간/URL 머리글 공간 최소화
- 굴뚝·장비조건부터 인쇄 2페이지 강제 시작
- 프린트 시 선택된 흡인노즐 버튼만 진한 색으로 강조
- 오리피스압차 평균 / K-factor / 등속흡인계수를 한 줄 3칸으로 정렬
- 적산유량계 채취 전/후/차 및 누출검사 영역 확대
- 입자상 측정조건 / 먼지 시료채취시간을 왼쪽 정렬
- 계산/세팅/장비값/측정점 로직 변경 없음


## v31
- 프린트에서 오늘 작성한 기록/기록목록 완전 숨김
- 앞쪽 기록지 글자 크기 확대(라벨 약 9pt, 값 약 9.3pt)
- 검정선 위주의 팩스 느낌을 줄이고 연한 블루/그레이 문서형 디자인 적용
- 섹션 제목을 문서 헤더 스타일로 정리
- 굴뚝·장비조건은 2페이지 시작 유지
- 선택 노즐만 진한 색으로 명확하게 표시
- 자동계산 3칸과 적산유량계/누출검사 영역을 더 크게 정리
- 입자상/가스상 표 글씨 및 행높이 확대
- 로우데이터는 기존 만족 상태를 최대한 유지


## v32 - 기존 DFEN XLSM 불러오기
- 기존 회사 시료채취기록지 `.xlsm`의 숨김 `기록부` 시트를 원시 인자값 기준으로 인식
- 업체명 D4 / 시설명 E5 / 측정일 D6
- 피토관계수 S4 / 기온 S5 / 대기압 S6 / 측정위치대기압 V7
- 노즐직경 E8 / 굴뚝내경 T8 / 날씨 Y8 / 습도 Y9 / 풍속 Y10 / 풍향 Y11
- O2 E10 / CO2 D11 / 수분 E13 / 여지번호 R13 / 적산유량계 전 Y13
- 입자상 지점별 값은 기록부 18~22행의 온도/정압/동압/진공압/미터온도/임핀저/채취량/시간을 자동 복원
- 가스상은 기록부 31~47행 중 실제 측정값이 있는 행만 자동 추가
- 기존 웹 출력 Excel(__WEB_DATA)과 v18+ 출력형식 불러오기도 그대로 유지


## v33 - 기존 XLSM 양식 그대로 출력
- 사용자가 업로드한 기존 `기록지(먼지)` XLSM 파일을 `dreampoen_record_template.xlsm`로 그대로 포함
- Excel 출력 시 새 통합문서를 만들지 않고 템플릿 ZIP/XML의 값 셀만 수정
- 열너비/행높이/병합셀/색상/인쇄영역/매크로(vbaProject)를 변경하지 않음
- 숨김 `기록부` 원시값과 보이는 `기록지(먼지)` 값을 동시에 배치
- 출력형식 `.xlsm` 유지, Excel 재계산을 열 때 강제
- 기존 XLSM 업로드 불러오기 기능 유지
- 제목 번호: 1 현장측정정보 / 2 기상 / 3 산소·CO2 / 4 수분 / 5 굴뚝·장비 / 6 입자상 / 7 가스상 / 8 자동계산 / 9 산출근거
- 업체명/시설명 입력란을 전체 폭으로 확대


## v34 - 기존 XLSM 출력 안정화
- 숫자 셀에 NaN/undefined/null이 절대 기록되지 않도록 공백 처리
- 담당자/담당자(부)/책임기술자 이름에서 기존 `(인)` 문구를 제거 후 1회만 추가
- 산소량/CO2/등속흡인계수/노즐직경/굴뚝 치수 단위를 기존 양식 셀에 표시
- 기록지의 유속/유량 값은 라벨 셀이 아닌 실제 값 셀로 매핑 수정
- 적산유량계/누출검사 셀 매핑 수정
- 1~5지점 측정점 위치를 웹 산출결과에 맞춰 기존 기록지에 자동 입력
- 웹 측정점 SVG를 스타일 포함 PNG로 변환하여 기존 Excel 중앙 그림 위치에 삽입
- 등속계산 탭에 수분, 밀도, 유속, Vic, I factor, 유량 등 계산 가능한 인자값/산출값 입력
- workbook.xml을 재작성하지 않아 기존 VBA/매크로/통합문서 구조 손상을 최소화


## v35
- 측정점 그림 Excel 삽입 크기: 너비 8.7 cm × 높이 6.9 cm 고정
- 웹에서 출력한 XLSM의 숨김 `기록부!AZ200`에 전체 웹 기록 JSON을 함께 보존
- 따라서 출력한 XLSM을 다시 `Excel 불러오기` 했을 때 셀 몇 개만 재구성하지 않고 전체 상태를 우선 복원
- 담당자/담당자(부)/책임기술자, 전체·먼지 채취시간, 원형·사각형 선택 및 치수, 팀/노즐 선택, 수분/O2/CO2 배열, 1~5지점, 가스상 추가행 등 재업로드 정확도 개선
- 기존 회사 XLSM처럼 AZ200 상태가 없는 파일은 기존 `기록부` 셀 매핑 방식으로 계속 불러옴
- 가스상 legacy 재업로드 범위를 기록부 30~47행으로 보정


## v36
- Excel 측정점 그림 크기 8.7cm × 6.9cm 유지
- 기존 템플릿의 측정점 원형 도형 중심을 기준으로 새 PNG 위치 재조정
- 그림을 좌측/상단으로 이동하여 기존 측정점 틀 안에 들어가도록 배치
- 웹→XLSM→웹 재업로드, 셀 매핑, 등속계산 값, 팀/노즐/측정점 계산은 v35 그대로 유지


## v37
- 굴뚝내경 Excel 배치 분리: 원형은 기존 상단 내경칸, 사각형은 상단칸을 비우고 아래 2칸에 가로/세로 각각 입력
- 수분량 셀 및 출력 XML에서 `#REF!` 오류가 노출되지 않도록 정리
- 누출검사 결과는 템플릿의 기존 가운데정렬 스타일을 보존하도록 값만 입력
- v36의 측정점 그림 8.7 × 6.9cm 틀맞춤, 재업로드, 등속계산 등 기존 기능 유지


## v38
- 측정점 PNG를 고정 크기/오프셋이 아니라 기록지의 실제 셀 틀(J9~P22)에 고정
- Excel drawing anchor를 twoCellAnchor로 변경하여 틀의 좌상단/우하단 경계에 맞춤
- 재출력 시 이전 자동그림이 중복되지 않도록 기존 생성 그림 제거 후 1개만 삽입
- v37의 Excel 매핑 및 웹 재업로드 기능은 그대로 유지


## v39
- v38의 J9~P22 전체 셀범위 확대 배치를 제거
- 측정점 그림을 사용자가 확인한 기존 크기 8.7cm × 6.9cm로 복원
- Excel 중앙 측정점 사각형 위치에 고정 배치
- 나머지 v38 기능/계산/재업로드/등속계산은 변경 없음


## v40 - 사용자가 맞춘 Excel 그림 위치 그대로 적용
- 사용자가 업로드한 `20260826-12_주식회사 삼손종합정비_시료채취기록지 1.xlsm`의 실제 그림 앵커값을 직접 확인하여 적용
- 시작 위치: col 9 / colOff 23946 / row 8 / rowOff 341150
- 그림 크기: cx 3132000 / cy 2395122
- 즉, 더 이상 셀 범위나 추정 좌표로 맞추지 않고 사용자가 Excel에서 확인한 위치와 동일하게 삽입
- 계산, Excel 재업로드, 등속계산, 원형/사각형 및 기타 v39 기능은 변경하지 않음


## v41
- 사각형 굴뚝 치수 Excel 배치 수정
- 사각형일 때 G19=가로, I19=세로로 같은 줄에 나란히 출력
- 기존 세로값을 G20에 넣으면서 수분 흡습관법 값에 덮어써지던 문제 수정
- 원형은 기존 원형 내경 칸만 사용
- v40의 측정점 그림 위치 및 나머지 기능은 그대로 유지


## v42
- 굴뚝내경 사각형 배치 재수정
- 가로값: G19
- 세로값: H19 (기존 단위칸 I19에서 한 칸 왼쪽으로 이동)
- 단위 m: 위쪽 I18 / 아래쪽 I19에 각각 표시
- v40에서 확정한 측정점 그림 위치와 나머지 기능은 그대로 유지


## v43 - 통합관리 좌측 메뉴 1단계
- 기존 v42 시료채취기록지 기능/계산/Excel 출력 코드는 변경하지 않음
- 화면 왼쪽에 고정 사이드 메뉴 추가
- 메뉴 순서: 업체관리 → 일정관리 → 일정등록 → 시료채취기록지
- 현재 시료채취기록지는 기존 화면 그대로 표시
- 업체관리/일정관리/일정등록은 다음 단계 연결을 위한 빈 화면만 생성
- 인쇄 시 사이드 메뉴는 출력되지 않고 기존 시료채취기록지만 출력


## v44 - 업체관리 1차 완성
- 기존 공용출장일정 통합관리 v2.7의 `shared_db.json` 업체 343개를 웹 데이터로 이관
- 검색: 상호/주소/대표자/환경기술인/사업자번호/연락처
- 관리연도 및 월 선택
- v2.7과 동일한 월별 `1차 / 2차`, 상반기 측정일, 하반기 측정일, 연간관리상태 표시
- 측정주기 판정: 반기/연2회, 연1회, 분기/4회, 월/12회
- 미측정 상태 빨간 경고, 연간완료 녹색 표시
- 업체 상세정보 및 시설별 측정정보 조회
- 업체 기본정보 수정/신규 업체 추가
- 시설 추가/삭제, 시설별 측정주기 및 측정항목 관리
- 브라우저 수정 데이터는 localStorage에 보존, DB JSON 내보내기 지원
- 시료채취기록지 내부 코드는 변경하지 않음
- Excel 템플릿은 사용자가 새로 수정한 `dreampoen_record_template.xlsm`으로 교체


## v45 - 업체 기본정보 모달 수정
- 업체 선택 후 `기본정보 수정`을 눌렀을 때 빈 모달처럼 보이거나 닫기(X)가 동작하지 않던 문제 수정
- 모달 내부가 동적으로 다시 생성될 때 닫기/취소 이벤트가 끊기던 문제 해결
- X / 취소 / 모달 바깥 영역 클릭 모두 정상적으로 닫힘
- 업체관리 데이터 구조와 시료채취기록지 기능은 변경하지 않음


## v46 - 측정인 상·하반기 이력 통합
- 상반기측정일.xlsx 732건 + 하반기측정일.xlsx 186건, 총 918건 반영
- 사업자번호 → 주소 → 업체명 순으로 기존 업체와 매칭
- 기존 DB에 없던 업체 2개 자동 추가
- 기존 DB와 맞지 않던 측정시설 56개 시설별 측정정보에 추가
- 측정인 측정항목을 업체/시설 측정항목에 합쳐 누락 항목 보완
- 측정이력 918건을 업체 및 시설 이력으로 저장
- 업체관리 탭: `전체 / 1월 / 2월 ... / 12월` 추가
- 전체 탭은 업체별 12개월 측정일·시설수·측정건수 및 상·하반기/연간상태를 한 화면에서 확인
- 월 탭은 기존 1차/2차와 상·하반기 상태를 상세 표시
- 상세정보 하단에 측정인 측정번호/시설/항목/상태/기술인력 이력표 추가
- 연간관리상태는 시설별 측정주기와 실제 시설별 측정이력을 함께 확인하도록 개선
- 시료채취기록지 기능은 변경하지 않음


## v47 - 일정관리 달력
- 기존 `원본_출장일지_및_일정_26년.xlsx`의 2026 월별 일정 469행을 읽어 동일 날짜·내용의 담당자를 묶어 366개 일정으로 이관
- 기존 측정인 완료 이력과 날짜가 일치하는 연결업체 일정은 완료상태로 자동 표시
- 월 달력: 월~일 7열 × 6주, 하루 최대 4개 일정 + 추가건수 표시
- 상태 3단계: ○ 예정 / ● 확정 / ✓ 완료
- 일정관리 하단: 선택 날짜 전체 일정 목록
- 일정 확정/확정취소/측정완료/완료취소/삭제 기능
- `측정출장` 완료 시 연결된 업체의 MeasurementHistory 및 월별 1차/2차 Tracking을 자동 갱신
- 완료 취소 시 해당 일정으로 생성된 측정이력만 제거하고 월별 상태 재계산
- `이 날짜 일정 등록` 클릭 시 일정등록 메뉴로 이동하고 선택 날짜를 임시 저장
- 업체관리 v46 및 시료채취기록지 기능은 변경하지 않음


## v48
- 일정관리 재편성: 기존 측정출장 일정은 제거하고 상·하반기 측정인 실제 측정이력 기준으로 381개 일정 재생성
- 실제 측정자료 일정은 전부 `● 확정 + ✓ 완료` 처리
- 기존 연차/휴가 일정 20개는 그대로 유지
- 업체 매칭: 사업자번호 → 주소 → 업체명 순서로 강화, 미매칭 그룹 0개
- 시료채취기록지 Excel 출력 시 표준산소농도(stdO2) 저장 및 표시
- 등속계산 탭 원본 산소보정 수식이 참조하는 `AF30`에 표준산소농도 자동 입력
- 기록지 출력본 빈 구분행에 `표준산소 O₂` 및 값/% 표시 (셀 크기/행 높이 변경 없음)
- 시료채취기록지 업체명에 업체관리 데이터 선택목록 연동
- 업체 선택 후 시설명 선택목록을 해당 업체 시설로 자동 제한
- 선택한 업체/시설 ID를 hidden 필드로 저장하여 다음 단계 굴뚝 직경/가로·세로 연동 기반 마련
- 직접입력 기능도 계속 허용
- 기존 Excel 그림 위치, 계산, 업체관리 기능은 변경하지 않음


## v49
- 일정등록 완성: 날짜/구분/담당자/상태/업체/시설/측정항목/일정내용/비고 → 달력 즉시 반영
- 완료 상태 측정출장은 업체 측정이력 자동 반영
- 업체관리 `하반기 측정 미완료만` 필터 추가
- 시설별 측정정보에 굴뚝 단면/내경/가로/세로 입력란 추가
- 시료채취기록지 업체→시설 선택 시 등록된 내경/치수 자동입력
- 기존 DB의 시설 내경 자료는 원형 0건, 사각형 0건으로 현재 미등록 상태
- Excel 기록지(먼지) `유량 (산소보정 후) (Sm3/min)` 값 셀 L25에 correctedFlow 자동 입력


## v50 - 모바일/태블릿 반응형
- PC 화면, 계산, Excel 출력, 업체/일정 데이터 구조는 변경하지 않음
- 768px 이하 휴대폰에서는 좌측 메뉴를 ☰ 서랍형 메뉴로 변경
- 모바일 메뉴는 메뉴 선택 후 자동 닫힘
- 업체관리 검색/필터/월탭/요약카드 모바일 재배치
- 업체관리의 긴 표는 좌우 스크롤
- 일정관리 달력은 7일 구조를 유지하면서 모바일 좌우 스크롤 지원
- 일정 상세 액션버튼을 터치하기 쉬운 2열 버튼으로 정리
- 일정등록은 모바일 1열 입력폼
- 시료채취기록지는 모바일에서 입력칸을 한 열로 배치하고 입력 높이/글자 확대
- 입자상/가스상/로우데이터의 넓은 표는 원래 열 구조를 유지하고 좌우 스크롤
- 모바일에서 업체/시설 편집 모달을 화면 폭에 맞게 조정
- 인쇄 시 모바일 메뉴는 출력되지 않음


## v51
- 업체관리 첫 열 `상호(기관명)`을 가로 스크롤 시 고정(sticky) 처리
- `06월 16일` 같은 기존 Tracking 날짜도 2026-06-16으로 정상 인식
- 상·하반기 표시를 시설명 매칭이 아닌 실제 업체 측정일(측정인 이력 + Tracking) 기준으로 재판정
- 하반기 미완료 필터도 하반기 측정이 필요한 업체 중 실제 하반기 날짜가 없는 업체만 표시
- `출장일지 및 일정 26년(1).xlsx`의 `출장일지` 시트에서 날짜는 제외하고 시설 기준정보만 반영
- 업체 343개 / 시설 1083개 기준정보 매칭 완료
- 원형 내경 749개, 사각 가로·세로 228개 자동 입력
- 내경/치수 미등록 시설 106개
- 시설별 설비명, 배출시설, 측정항목, 항목별 측정주기(ItemCycles) 갱신
- 업체 상세 시설표에 원형/사각 및 내경/가로×세로 표시
- 일정등록에서 한 업체의 시설을 여러 개 `시설 추가` 방식으로 선택 가능
- 선택 시설들의 측정항목을 자동 합산하여 일정에 저장
- 시료채취기록지에서 업체→시설 선택 시 v51에 입력한 원형 내경 또는 사각 가로·세로 자동연동 유지
- 측정일은 상반기/하반기 측정자료를 계속 기준으로 사용하며 출장일지 날짜는 반영하지 않음


## v52
- Excel `기록지(먼지)`에서 입자상 측정조건 위에 표시되던 `표준산소 O₂` 라벨/값 숨김
- 표준산소농도 계산값 자체는 숨김 기록부 AA200 및 등속계산 AF30에 그대로 유지
- 따라서 산소보정 유량 계산/Excel 연동 기능은 유지
- 모바일(768px 이하)에서 O2/CO2/수분 반복 입력칸을 세로형으로 배치
- PC 화면, 업체관리, 일정관리, 일정등록, 시료채취기록지 계산/Excel 그림 위치는 변경하지 않음


## v53
- 일정등록 시설 선택 목록을 `방지시설명`만 표시하도록 단순화
- 배출시설명/보조 라벨을 시설 선택 목록에서 제거
- 동일 방지시설명이 여러 개 있어도 아직 추가하지 않은 시설을 우선 선택해 복수 추가 가능
- 모바일 업체관리 화면을 업체목록(상단) + 상세/설비현황(하단) 분할 구조로 변경
- 모바일 상세/설비현황 영역은 하단 약 46%에 고정되고 내부만 스크롤
- 업체 목록도 상단 영역 안에서 별도로 스크롤
- 시료채취기록지 시설 선택 시 직경 연동 오류 수정
- 기존 코드가 존재하지 않는 updateShapeUI()를 호출하던 문제를 실제 syncStackShape()로 교체
- 원형 시설 선택 시 내경, 사각 시설 선택 시 가로/세로 자동 입력 후 측정점/계산 재계산


## v54
- 시설 DB 중복 ID 461건 정리. 동일 업체 안 모든 시설 ID를 고유값으로 보정
- 시설 표시명을 방지시설명(PreventionFacility) 기준으로 통일
- 에스카모터스 방1/방2처럼 기존에 같은 ID로 저장되어 두 번째 시설이 추가되지 않던 문제 해결
- 시료채취기록지 업체명/시설명 입력 옆에 별도 `▼ 업체 선택`, `▼ 시설 선택` 탭 추가
- 기존 입력값을 지우지 않아도 select 탭으로 바로 다른 업체/시설로 교체 가능
- 시설 선택은 텍스트 재검색이 아니라 시설 ID 기준으로 처리하여 내경 연동 정확도 개선
- 원형 시설 선택 시 Diameter, 사각형 선택 시 StackW/StackH 자동 입력 후 계산 재실행
- 일정등록도 업체/시설을 ID 기반 select로 변경하여 복수 시설 추가 안정화
- 왼쪽 브랜드 DREAMPOEN → DREAMFOREN 변경, DF 로고마크 추가
- 시료채취기록 상단 DREAMFOREN 표기로 통일
- 모바일 업체관리는 페이지 전체 스크롤을 다시 허용하고 업체목록 38vh / 상세·설비현황 42vh로 분리
- 모바일 업체명 첫 열 폭 165px 고정 및 sticky 강화
- 상세/설비현황은 하단 sticky 영역으로 유지하면서 내부 세로스크롤 가능


## v55
- 업체/시설 입력 UX를 `입력칸 + 별도 선택탭` 2개 구조에서 단일 검색형 입력칸으로 통합
- 입력칸 클릭 시 전체 업체/시설 목록 표시
- 글자를 입력하면 실시간 필터 검색
- 검색 결과 클릭 시 즉시 선택 및 DB ID 저장
- 기존 입력값을 삭제하지 않아도 클릭 후 검색/교체 가능
- 업체검색 결과에는 주소를 보조정보로 표시
- 시설검색 결과는 방지시설명만 표시
- 시료채취기록지 업체/시설 검색 UI 통합
- 일정등록 업체/시설 검색 UI도 동일한 방식으로 통일
- 일정등록 시설 복수 추가 및 ID 기반 중복방지 기능 유지
- 시설 선택 시 내경/가로·세로 자동 연동 기능 유지
- PC/모바일 동일한 검색 동작, 모바일은 터치 가능한 큰 검색결과 버튼 적용


## v56
- 센트럴모터스 포함 내경 미연동 DB 전체 재점검
- 단면 형상 값은 비어 있지만 DimensionRaw가 남아 있던 시설 3개 자동 추론
- 단일 숫자 내경은 원형, `가로 x 세로` 형식은 사각형으로 자동 변환
- 센트럴모터스 #1/#2 0.65 → 원형 내경 0.65m, 여과집진 0.8 x 0.8 → 사각 0.8×0.8m로 보완
- 원본에도 내경/가로세로 정보가 없는 시설 158개는 자동입력 불가 상태로 유지
- 일정등록 선택 시설을 가로형 `시설명 / 측정항목 / 측정주기 / 삭제` 행으로 변경
- 시설마다 측정항목을 개별 수정 가능 (예: 먼지, 면제)
- 시설마다 측정주기를 개별 설정 가능
- 일정 저장 시 해당 시설의 Items/Cycle/ItemCycles를 업체관리 DB에도 즉시 반영
- 업체관리의 전체 측정항목/측정주기도 시설 변경내용을 다시 집계
- 서로 다른 시설주기가 있으면 업체 측정주기는 `복합주기`로 표시
- 일정 데이터에도 FacilityPlans로 시설별 항목/주기를 함께 저장
- 같은 v56에서 사용자가 수정한 시설 항목/주기는 페이지 재접속 시 배포 DB에 의해 덮어써지지 않도록 병합 방식 개선


## v57
- 왼쪽 메뉴에 네비게이션 카테고리 추가
- 업체관리 DB 기반 실시간 업체 검색
- 업체 선택 시 업체명/주소만 간단히 표시
- 카카오맵 / 티맵 선택 버튼 제공
- PC/모바일 모두 지원
- 기존 관리/기록지 기능 유지


## v58
- 네비게이션 메뉴 클릭 시 빈 화면만 나오던 라우팅/표시 문제 보강
- 네비게이션 클릭 시 다른 view를 숨기고 네비게이션 view를 강제로 표시
- 업체 검색창과 주소 결과영역이 PC/모바일에서 항상 표시되도록 CSS 보강
- 모바일 네비게이션 화면 세로 스크롤 허용
- 기존 임시 DF 로고 제거
- 사용자가 제공한 드림포이엔 CI 이미지를 assets/dreamforen_ci.jpg로 포함
- PC/모바일 왼쪽 브랜드 영역에 실제 CI 로고 표시


## v59
- TMAP 버튼이 앱만 열리고 업체 주소가 전달되지 않던 문제 수정
- 주소 문자열만 있는 업체 DB에서는 좌표 기반 route 스킴 대신 `tmap://search?name=주소` 사용
- 모바일에서 TMAP 앱 실행 시 업체 주소를 티맵 검색창에 자동 전달
- 앱 실행이 되지 않거나 custom scheme이 차단되면 약 1.4초 후 TMAP 웹 검색으로 자동 폴백
- 카카오맵 주소 검색 연동은 기존 정상 동작 유지
- 네비게이션 화면에 TMAP 연동 방식 안내문 추가


## v60 - 분석 1차
- 먼지/중금속 시료채취기록지에서 공통으로 사용할 분석항목 23개 추가
- 추가항목: 브로민화합물, 비소화합물, 카드뮴화합물, 납화합물, 크로뮴화합물, 구리화합물, 니켈화합물, 아연화합물, 아세트알데하이드, 베릴륨, 벤젠, 염화비닐, 디클로로메탄, 클로로포름, 1,2-디클로로에탄, 사염화탄소, 트리클로로에틸렌, 테트라클로로에틸렌, 에틸벤젠, 스타이렌, 1,3-부타디엔, 아크릴로니트릴, 아닐린
- 왼쪽 메뉴에 `분석` 카테고리 추가
- 시료채취기록지에 등록된 측정항목을 기준으로 필요한 분석 로우데이터 카드만 자동 생성
- 예: 먼지 + 황산화물 + 구리화합물 + 벤젠 + 포름알데하이드 선택 시 해당 5개 계산서만 생성
- 분석 로우데이터는 Excel 추출 없이 화면에서 바로 A4 세로 인쇄/PDF
- 각 분석카드는 `계산식 / 실측값 기입란 / 결과` 구조
- 먼지, 황산화물, 포름알데하이드, 구리화합물은 1차 전용 계산식 레이아웃 적용
- 금속류/VOC류는 공통 1차 계산서 틀 적용
- 다음 단계에서 실제 공정시험기준별 정확한 분석식·희석배수·검량선·바탕시험·단위환산을 항목별로 세분화할 수 있도록 구조화


## v61 - 먼지 분석 1차 완성
- 네비게이션/분석/업체관리/일정관리/시료채취기록지를 `.df-view`로 완전히 분리
- 메뉴 이동 시 다른 모든 화면을 강제로 숨겨 시료채취기록지가 아래에 중복 표시되던 문제 수정
- 분석은 현재 작성화면이 아니라 `저장된 시료채취기록`을 선택해 생성하는 구조로 변경
- 분석 선택목록은 접수번호 / 업체명 / 시설명 기준 표시
- 등속흡인 관련 값이 저장된 기록은 가스상 선택 여부와 무관하게 먼지 분석서를 자동 생성
- 먼지 공식은 사용자 제공 공정시험식 그대로 적용:
  Cn = md / [ V'm × 273/(273+θm) × (Pa + ΔH/13.6)/760 ]
- md(채취된 먼지량)는 분석실 입력
- V'm, θm, Pa, ΔH는 저장된 시료채취기록에서 가능한 값 자동 불러오기
- Pa가 hPa로 저장된 경우 mmHg로 자동 환산
- 입력값이 갖춰지면 먼지농도 mg/Sm³ 자동 계산
- 실측값 대입식과 최종 결과를 A4 인쇄용 화면에 표시
- 이번 버전은 먼지만 완성하고 다른 분석항목 계산서는 의도적으로 생성하지 않음


## v62 - 카테고리 빠른 전환 오류 수정
- 메뉴를 빠르게 연속 클릭할 때 여러 카테고리 화면이 한 페이지에 동시에 쌓이던 현상 수정
- 화면 전환 로직을 `SINGLE VIEW ROUTER` 하나로 통합
- 클릭 즉시 모든 view를 숨긴 후 선택한 view 하나만 표시
- `hidden + active class + display!important` 3중 안전장치 적용
- requestAnimationFrame에서 한 번 더 최종 화면 상태를 검증하여 이전 이벤트가 뒤늦게 화면을 다시 열지 못하게 처리
- capture 단계에서 이전 버전의 중복 메뉴 이벤트 전파를 차단
- PC/모바일 동일 적용
- 업체/일정/네비게이션/분석/시료채취 데이터 및 계산 로직은 변경하지 않음


## v63 - 먼지 분석 저장연동/계산 개선
- 분석 저장기록을 실제 시료채취 저장소 `dreampoen_sample_records_v17`의 `data.fields` 구조로 직접 연결
- 접수번호 / 업체명 / 시설명이 `업체명 없음`으로 표시되던 문제 수정
- 분석 기록 목록은 실제 시료채취기록 저장순서/갱신시각 기준으로 표시
- 먼지 분석 표를 5열 구조에서 컴팩트 2열 입력 구조로 축소
- 단위는 값 입력칸 옆에 고정 표시
- 채취 전 여지무게 / 채취 후 여지무게 입력란 추가
- md = 채취 후 무게 - 채취 전 무게 자동계산
- 최종 먼지농도는 md 차이값을 사용해 자동 계산
- 시료채취기록의 points.volume 합계 → V'm(m³) 자동 연동
- 건식가스미터 입구/출구 평균 → θm 자동 연동
- 측정위치 대기압 → Pa 자동 연동
- 저장된 지점값과 장비조건을 이용해 오리피스 압력차 ΔH를 기록지와 동일식으로 재계산/자동 연동
- 입력값은 공식 안에 `md (값 mg)`, `V'm (값 m³)`, `θm (값 ℃)`, `Pa (값 mmHg)`, `ΔH (값 mmH₂O)` 형태로 즉시 삽입
- 단위는 공식 안에서도 유지
- 최종결과를 계산식 바로 아래에 mg/Sm³로 표시
- 실측값 대입란에는 전후무게 차이 및 전체 수치대입식을 함께 표시


## v64 - LAB Raw Data 개선
- 분석 메뉴명을 `LAB Raw Data`로 변경
- 같은 접수번호/업체/시설/기록종류의 저장기록은 최신 1건만 표시하여 설비 1건이 분석에서 누적되어 보이는 현상 방지
- 먼지 채취 전/후 여지무게 입력단위를 mg → g로 변경
- md = (후 여지무게[g] - 전 여지무게[g]) × 1,000 으로 mg 자동 환산
- 먼지 최종결과는 소수점 첫째자리(mg/Sm³)로 표시
- 각 기호 단위는 계산식 바로 아래에 별도 단위행으로 배치
- 분석실 입력한 전/후 여지무게는 레코드별 별도 캐시에 저장
- 저장기록 새로고침을 눌러도 기존 분석실 입력값이 사라지지 않음
- 시료채취기록지 input/change 발생 시 autosave 후 LAB Raw Data를 약 0.35초 뒤 자동 동기화
- 가스상 항목을 추가/삭제하면 선택된 LAB 기록의 분석대상 항목 목록도 자동 갱신
- 이황화탄소를 분석항목 인식 목록에 추가
- 먼지 외 추가 분석항목은 LAB 화면에 '분석 로우데이터 연동대상' 카드로 즉시 표시


## v65 - 저장구조/LAB 기준 정리
- 현재폼초기화 시 현재 폼 + 모든 시료채취 저장기록 + 자동저장 초안 + LAB 입력 캐시까지 전체 삭제
- 시료채취기록의 `data`를 확정 저장본, `autosaveData`를 복구용 작업본으로 명확히 분리
- 자동저장은 더 이상 확정본 저장시각/업체명/시설명/오늘 기록 목록을 바꾸지 않음
- 오늘 작성한 기록 목록은 autosaveData가 아닌 확정본 data만 표시
- 저장기록 열기/삭제 라벨도 확정본 data 기준
- LAB Raw Data는 `기록 저장`을 누른 확정본 data만 조회
- 같은 업체의 다른 시설에서 작성 중인 미저장 내용은 LAB에 표시되지 않음
- 네비게이션 / LAB Raw Data 메뉴 앞 아이콘 제거
- V'm / θm / Pa / ΔH는 readonly 처리하여 LAB에서 수정 불가
- 위 값은 시료채취기록지에서 수정 후 `기록 저장`해야만 LAB에 변경 반영
- 먼지 계산식 바로 아래에 기호 / 항목 / 단위 설명표 배치
- 기존 계산서 하단 기호 설명블록 제거
- 먼지 최종결과 소수점 첫째자리 유지


## v66
- 업로드된 `20260731-01_현대자동차정비공장_시료채취기록지.xlsm` 실제 셀배치를 기준으로 Excel/XLSM 불러오기 재작성
- Excel 날짜 Date 객체를 UTC로 변환하던 하루 차이 가능성 제거, 로컬 날짜 그대로 복원
- Excel serial date도 YYYY-MM-DD로 변환
- 접수번호 G2 / 측정날짜 Q2 / 업체명 G3 / 시설명 G5 복원
- 담당자(정) N6 / 담당자(부) P6 / 책임기술자 Q6 복원
- 전체 채취시간 G7~I7, 먼지 시료채취시간 G29~I29 복원
- 사각형 내경 G19/H19, 원형 내경 G18을 실제 형상에 맞춰 복원
- 입자상 로우데이터 32~36행을 현재 템플릿 열배치 기준으로 복원
- 여과지 홀더온도 L32:L36 복원
- 가스상 항목은 측정값이 비어 있어도 항목명이 있으면 반드시 복원
- 가스상 시간 K/R열의 `시작-종료` 값을 start/end로 분리 복원
- 수분 원시값은 등속계산 U10:U14 값이 평균과 일치할 때 1~5차 모두 복원
- O2/CO2 원시값은 파일 내 raw 범위 평균이 표시 평균과 일치할 때 전부 복원
- 구형 파일에 raw O2/CO2가 신뢰할 수 없으면 표시 평균을 기존 입력칸 전체에 채워 한 칸만 채워지는 현상 방지
- 최신 웹 출력 XLSM은 AZ200 전체 상태를 최우선 사용하므로 O2/CO2/수분 개수와 값이 그대로 복원
- LAB `기호/항목/단위` 표 폭과 글자크기 재조정
- 시료채취기록에서 가스상 항목을 추가/삭제 후 `기록 저장`하면 선택 중인 LAB 기록에 즉시 재반영
- LAB 카테고리 재진입 시 선택 중인 저장기록을 최신 확정본으로 자동 재로딩


## v68
- 관리자 계정 최상단에 계약관리 추가
- 직원 계정에서는 계약관리 메뉴 숨김 + Supabase RLS로 contracts 직접 조회 차단
- 2024~2026 계약 473건은 별도 SQL로 Supabase에 일괄 등록
- 계약관리: 연도/상태/검색, 계약금액, 만료예정, 재계약 필요, 상세보기
- 업체관리 명칭을 업체현황으로 변경하고 현재 유효 계약업체만 표시
- 일정등록/시료채취/네비게이션 업체 선택도 현재 계약업체 기준
- 일정관리에서는 완료된 과거 일정은 보존하고, 미완료 일정은 현재 계약업체 기준으로 표시
- 업체/시설 master를 Supabase companies/facilities로 최초 자동 업로드 후 온라인 기준으로 다시 불러옴
- 기존 localStorage는 보조 캐시/기존 기능 호환용으로 유지

### 최초 1회 필수
Supabase SQL Editor에서 별도 제공되는 `supabase_v68_contracts_import.sql`을 실행한 뒤 v68을 새로고침하세요.
이 SQL은 업체/시설 온라인 동기화 컬럼 보강과 contracts 테이블/RLS 생성, 473건 계약자료 입력을 함께 처리합니다.


## v69
- 계약관리 표를 컴팩트하게 조정하고 측정대상사업장/계약명을 최대 2줄로 표시
- 계약 및 업체 상세내용을 화면 하단이 아닌 클릭형 팝업으로 통합
- 빈 하단 시설현황/상세영역 제거
- 계약금액 기준(1년/반기/분기/월/건별/기타/확인필요) 표시 및 관리자 수정/저장
- 계약체결 누적현황 CSV, 계약 1건 CSV, 계약정보 요약 출력/PDF 추가
- 표준계약서/계약체결사실통보서 자동생성은 정확한 HWP 입력칸 매핑 미확인으로 이번 버전에서 제외


## v70
- 계약 팝업을 관리자 직접 수정형으로 전환
- 계약 추가/수정/삭제 및 Supabase 즉시 저장
- '원본상태' 표시 제거, 대신 실무용 '계약상태' 편집
- 계약금액 기준, 계약일/과업기간/금액/기관/사업장/주소/종별/관할기관 편집
- 업체 담당자/연락처/메모 추가
- 향후 계약 항목을 자유롭게 늘릴 수 있는 사용자 정의 추가항목(key/value) 저장
- 계약 저장/삭제 후 현재 계약업체 목록을 즉시 재계산하여 업체현황·일정·시료채취·네비 기준에 반영
- 업체현황 온라인 새로고침 및 온라인 저장 상태 표시
- 업체/시설 편집 결과 Supabase 저장 연동 유지 및 상태 표시
- 네비게이션은 현재 계약 진행 업체만 사용하며 온라인 새로고침/주소복사 추가


## v71
- 업체현황은 현재 계약 진행 업체만 표시하는 기존 온라인 계약 기준 유지
- 업체 행 클릭 시 업체 기본정보, 시설별 정보, 항목별 측정주기, 첨부문서를 한 팝업에서 확인
- 업체 기본정보 수정 기능 유지
- 시설별로 방지시설명/배출시설명/굴뚝단면/메모 수정
- 한 시설 안에서도 측정항목마다 연 1회/반기 1회/분기 1회/월 1회/월 2회/직접설정 등 서로 다른 측정주기 지정
- 신고증명서/사업자등록증/기타 파일을 업체별로 직접 업로드
- PDF/JPG/PNG 등 브라우저 지원 형식은 앱 안에서 미리보기, 그 외 파일은 새 창 열기
- 첨부문서는 Supabase Storage private bucket(company-documents) + company_documents 테이블 사용
- v71 최초 1회 supabase_v71_company_documents.sql 실행 필요


## v72 긴급수정
- 업체현황 0건 문제: 온라인 current_contract_companies_safe 결과가 업체 DB와 0건 매칭될 때 기존 정상 계약업체 기준을 덮어쓰지 않도록 수정
- 계약진행 업체 기준 상태와 매칭 개수를 업체현황 상단에 표시
- 업체현황이 비어도 원인을 알 수 있도록 빈 화면 대신 안내행 표시
- 계약관리 UI 이벤트 초기화 함수가 누락된 문제 복구
- 계약추가/새로고침/필터/CSV 버튼 이벤트 재연결
- 계약 팝업 X, 취소, 바깥영역, ESC 닫기 모두 보강
- 과거 capture 이벤트가 클릭을 삼켜도 닫기 동작이 우선 실행되도록 안전 라우터 추가

## v73 업체현황 복구
- current_contract_companies_safe 뷰 의존 제거
- Supabase contracts 테이블의 현재 계약진행/만료예정 계약을 직접 읽어 업체현황 구성
- 기존 companies DB와 사업자번호/업체명으로 매칭
- 기존 업체DB에 없는 계약업체도 계약정보로 임시 행을 만들어 빈 화면 방지
- 임시 업체에서 업체정보/시설정보 수정 시 실제 companies DB 업체로 자동 등록 후 Supabase 저장
- 업체현황 온라인 새로고침 시 contracts → companies 직접 재매칭


## v74 업체현황 UI 복구
- 업체현황 section의 중복 class 속성 제거
- 전체~12월 탭, 검색/연도 필터, 활성업체/완료/미측정/시설수 카드 강제 표시
- 브라우저 캐시/과거 CSS 때문에 요소가 숨겨져도 런타임에서 display/visibility 복원
- 관련 DOM이 누락된 경우 화면 진입 시 자동 재생성
- 업체현황 진입/렌더 시 UI 무결성 검사 실행
- SQL 변경 없음


## v75 업체현황 전면 재구성
- 기존 업체현황 DOM/표시 복구 코드에 덧대는 방식 중단, 업체현황 화면 자체를 새로 구성
- 계약관리의 현재 계약 진행업체를 직접 기준으로 업체현황 표시
- 하반기 측정 미완료 필터 제거
- 진행상태 필터: 전체 / 측정주기 미완료 / 연간 완료 / 시설·주기 미등록
- 전체~12월 월별 조회 유지
- 업체추가/수정 팝업에서 시설을 여러 개 카드 형태로 추가/삭제
- 시설마다 방지시설명/배출시설명/단면/기본주기 별도 관리
- 시설마다 측정항목을 여러 개 추가하고 각 항목별 주기 별도 지정
- 기존 업체 첨부문서(신고증명서/사업자등록증/기타) 유지
- SQL 변경 없음

## v76 업체 DB 연결 수정
- 원인: index.html을 더블클릭(file://) 실행하면 브라우저가 fetch('./data/shared_db.json')를 차단하여 업체 DB가 0건이 됨
- data/shared_db.json의 345개 업체/기존 일정 DB를 app.js 내부에도 안전 fallback으로 내장
- file:// 실행 시 내장 DB를 즉시 사용
- http/https 실행 시 기존 shared_db.json 로드 유지
- Supabase companies 테이블이 비어 있으면 관리자 계정에서 내장 DB를 최초 업로드한 뒤 다시 온라인 DB를 읽도록 bootstrap 순환 의존 제거
- SQL 추가 실행 없음

## v77 업체현황 측정주기/첨부문서 개선
- Supabase Storage Invalid key 대응: Storage object key는 영문/숫자 기반으로 생성하고, 원본 한글 파일명은 metadata에 보존
- 업체현황 사업자번호 열 제거
- 측정 진행상태를 숫자(4/8 등) 대신 완료 / 미완료 / 측정확인필요로 단순화
- 연/반기/분기/월1회/월2회 주기를 현재 날짜 기준 '도래한 기간까지만' 판정
- 분기/월 측정이 아직 미래 기간이라는 이유로 미완료 처리되던 문제 제거
- 업체현황에 최근 측정일 표시
- 시설별 비고/특이사항 입력란 추가, 업체현황에 시설명과 함께 요약 표시
- 시설별 ItemCycles의 주기를 업체 복합주기 집계에도 반영
- SQL 변경 없음

## v78 업체현황/일정관리
- 업체현황 상단 중복 상태탭 제거
- 요약카드 순서: 등록 시설 / 계약 진행업체 / 미완료 / 측정확인필요 / 완료
- 요약카드를 클릭하면 즉시 해당 업체만 필터링
- 등록 시설 카드는 전체 업체를 시설 수 많은 순으로 정렬
- 계약 진행업체 카드는 전체 계약 진행업체 보기
- 일정관리 달력을 일→월→화→수→목→금→토 순서로 변경
- 일요일 시작 기준으로 월 시작 위치와 주말 색상 계산도 함께 수정
- SQL 변경 없음

## v79 저장/진행상태/일정등록 수정
- 업체 저장 시 Supabase numeric 컬럼에 빈 문자열을 보내던 오류 수정: capacity/stack height/diameter/stack_w/stack_h 빈값은 null 저장
- 시설 비고에 미측정/다음에 측정/고장/정지/휴업/추후/보류 등이 있으면 완료 상태를 측정확인필요로 자동 전환
- 비고에 미완료/기한초과/누락/미실시가 있으면 미완료로 자동 전환
- 일정등록의 전체 측정항목(자동) 입력란 제거
- 일정등록에서 시설을 선택하면 업체현황의 ItemCycles(법정주기)를 항목별로 그대로 가져옴
- 각 시설 안에서 분석/측정항목 추가/삭제 및 항목별 주기 수정 가능
- 일정에서 추가/수정한 항목/주기는 이번 일정에만 저장하고 업체현황의 법정주기 마스터는 덮어쓰지 않음
- FacilityPlans에 ItemCycles까지 저장
- SQL 변경 없음

## v80 일정등록/업체팝업
- 일정등록 화면을 새로 열 때 이전 일정의 업체/시설/항목/담당자/메모가 남지 않도록 자동 초기화
- 달력에서 특정 날짜의 [이 날짜 일정 등록]으로 들어갈 때는 선택 날짜만 유지하고 나머지 입력값 초기화
- 일정 저장 직후 숨겨진 등록 폼도 초기화하여 다음 일정 등록 시 이전 데이터 잔존 방지
- 업체 시설 팝업의 측정항목 입력칸 폭 축소
- 항목별 법정주기 옆에 최근 측정일 표시
- 최근 측정일은 측정이력의 시설명 + 측정항목을 우선 매칭하고, 항목 이력이 없으면 해당 시설의 최근 측정일로 보조 표시
- SQL 변경 없음

## v81 업체 측정일 직접관리 / 일정 보정
- 업체 시설의 항목별 주기에 따라 날짜 입력 UI 자동 변경
  - 연 1회: 1개 날짜
  - 반기 1회: 상반기/하반기
  - 분기 1회: 1~4분기
  - 월 1회: 1~12월 각 1개
  - 월 2회: 1~12월 각 2개
- 기존 측정이력이 있으면 수동 날짜값이 없을 때 자동 초기값으로 사용
- 진행상태는 위 입력 날짜를 기준으로 완료/미완료/측정확인필요 판정
- 업체현황 최근측정일 열 제거
- 업체현황 1~12월 탭에서 직접 입력 날짜까지 월별 측정일에 포함
- 일정관리 달력 선택 날짜가 일정등록의 일정일에 정확히 전달되도록 pending date 직접 전달
- 일정 저장 후 일정등록 화면에 그대로 머무름
- 업체현황 월 탭 클릭 이벤트를 화면 진입 때 다시 바인딩
- SQL 변경 없음

## v82 업체현황 단순화
- 업체현황 월별 탭 제거, 전체 화면만 사용
- 표 구성: 기관명 / 소재지 / 시설 / 측정진행상태 / 1~12월 측정일 / 시설비고
- 기관명·소재지 칸 폭 축소
- 월 2회 측정업체는 같은 월 칸에 날짜 2개까지 'MM-DD / MM-DD' 형태로 표시
- 기존 측정이력과 과거 수동 입력 날짜를 월별 날짜 표시에 반영
- 측정진행상태는 시설별 법정주기 + 실제 측정이력을 기준으로 완료/미완료/측정확인필요 판정
- 업체 팝업의 상반기/하반기/분기/월 날짜 입력 UI 전부 제거
- 팝업은 시설정보 + 측정항목 + 법정주기 + 비고만 관리
- SQL 변경 없음

## v83 업체현황 측정일 수정 / 일정완료 연동 / 열 너비 조절
- 일정관리에서 측정완료 처리 시 업체 및 시설 MeasurementHistory에 일정일 즉시 기록
- 완료 처리 후 Supabase 회사/시설 DB에도 즉시 동기화하여 업체현황 월별 날짜 갱신
- 완료취소 시 해당 일정의 업체/시설 측정이력도 제거
- 업체 팝업 시설별 [측정현황] 추가
- 날짜 입력 후 +날짜 추가 → 자동 오름차순 정렬, 중복 제거
- 기존 업로드 측정일도 목록에 표시하고 ×로 제외 가능
- 수동 수정한 시설은 ManualMeasurementDates를 기준으로 월별 현황 표시
- ManualMeasurementDates는 facilities.extra_data에 저장하므로 SQL 변경 없음
- 항목/주기/삭제 입력 영역 대폭 축소
- 업체현황 표 모든 헤더 오른쪽 경계를 드래그해 열 너비 조절 가능
- 열 너비는 브라우저 localStorage에 저장되어 다음 실행에도 유지

## v84 업체 측정현황 위치 / 시설 보존 / LAB 기호칸
- 시설별 하단 측정현황을 제거하고 업체 기본정보의 사업장 종 옆 영역으로 이동
- 측정현황에서 [시설 선택 + 날짜 + 날짜 추가] 방식으로 관리
- 날짜는 시설별로 저장되며 자동 정렬/중복 제거, 기존 날짜도 × 삭제 가능
- 측정완료 처리 시 전체 company/facility master를 다시 upsert하지 않고 measurement_history만 업데이트
- 완료 처리 후 방지시설명/배출시설명/측정항목/주기 등 마스터가 사라지는 문제 방지
- 일정의 CompanyIds가 오래되었어도 업체명으로 실DB 업체를 추가 확인
- LAB Raw Data 정의표의 기호 열 폭을 고정하고 중앙정렬, 단위 열도 정렬
- SQL 변경 없음

## v85
- 업체 진행상태 판정 완화: 현재 진행 중인 월/분기/반기는 아직 미측정이어도 측정확인필요로 만들지 않고, 이미 지나간 필수기간만 점검
- 시설 이력 매칭이 불완전한 기존 데이터는 업체 전체 측정이력을 보조로 사용하여 상·하반기 완료 업체의 오판정 감소
- 업체 팝업의 측정날짜 추가/삭제 UI 제거
- LAB Raw Data 전체 폭/계산식/기호표를 컨테이너 안에 강제 맞춤하여 우측 화면 넘침 방지
- 시료채취기록지 먼지/중금속 탭을 상단 제목 오른쪽 공간으로 이동
- 기록저장~현재폼초기화 버튼을 한 줄로 배치
- 기록목록을 하단 전체폭으로 배치하고 제목을 '오늘 작성한 기록'에서 '측정내용'으로 변경
- SQL 변경 없음

## v86 계약/업체현황 기준 전면 수정
- 업체현황은 Supabase contracts의 현재 유효 계약(active/expiring)만 기준으로 구성
- 계약DB 조회 실패 시에도 345개 전체업체로 fallback하지 않고 배포된 현재계약 안전목록(264 alias/250 사업자번호)만 사용
- 따라서 계약종결/만료 업체(예: 에스카모터스)가 업체현황에 다시 나타나는 문제 차단
- 계약관리 연도 필터를 source_year가 아니라 계약기간이 해당 연도와 겹치는지로 변경
  - 2024년에 등록된 계약이라도 2026년까지 계약기간이면 2026 화면에서 확인 가능
- 계약관리 상단 카드: 전체 / 계약중 / 60일내 만료 / 재계약필요 / 종결·중지·취소 / 금액
- 상태 카드 클릭 즉시 해당 계약만 필터
- 업체 측정상태는 시설별로 한 번 판정하며 혼합 항목주기는 가장 잦은 주기를 기준으로 확인
- 현재 월/분기/반기까지 측정여부를 반영하여 완료일 삭제 시 완료→측정확인필요로 즉시 변경
- SQL 변경 없음

## v87 동기화 실패 긴급수정
- 업체/시설 Pull, 계약DB 조회, 화면 렌더를 각각 분리하여 한 단계 오류가 전체 "동기화 실패"로 번지지 않게 수정
- 계약DB 조회 실패 시 현재계약 안전목록으로 자동 전환
- 업체 Pull 실패 시 브라우저에 이미 로드된 DB 유지
- 부분 성공 시 "부분 동기화 · 계약기준 사용" 또는 "로컬DB 사용중"으로 표시
- companyRender 내부 예외가 계약 fallback 재호출과 연쇄 실패를 일으키던 구조 제거
- 업체현황 진입 시 Promise 예외가 화면 전체 동기화를 실패시키지 않도록 보강
- SQL 변경 없음

## v88 측정 완료/완료취소 상태 연동
- 일정 완료/완료취소 시 로컬 업체/시설 측정이력을 먼저 변경하고 업체현황을 즉시 재계산
- 온라인 저장 실패가 발생해도 화면 상태 변경을 되돌리지 않음
- 온라인 저장 실패 시 "로컬 반영 완료 · 온라인 재시도 필요"로만 표시
- contractCompanies가 별도 객체를 들고 있어도 실제 company DB 객체로 참조 교체
- 반기/연1회 시설은 과거 시설명 매칭 오류를 보완하기 위해 업체 전체 측정이력을 함께 사용
- 월/분기 시설은 시설별 이력을 우선하여 다른 시설 날짜가 섞이지 않도록 유지
- 일정 삭제 시 완료취소 처리를 await한 뒤 삭제하도록 race condition 제거
- SQL 변경 없음

## v89 일정 완료/취소 즉시 화면반영
- 측정완료/완료취소 클릭 즉시 현재 선택 날짜와 일정 ID를 유지한 채 달력 + 일자목록 DOM 재렌더
- localStorage 저장보다 화면 렌더를 먼저 수행하여 큰 업체DB 직렬화로 인한 표시 지연 제거
- requestAnimationFrame 2단계 재렌더로 동일 클릭 프레임의 이전 DOM 잔상 방지
- 온라인 동기화 종료 후에도 선택 날짜/일정 유지 후 한 번 더 렌더
- 완료 상태면 측정완료 버튼 비활성 / 완료취소 활성, 취소 상태면 반대로 즉시 전환
- 다른 날짜를 클릭했다 돌아와야 상태가 보이던 문제 수정
- SQL 변경 없음

## v95.1 hotfix
- Supabase facilities.item_cycles가 JSON object 형태인 기존 마이그레이션 데이터를 앱 내부 표준 배열로 정규화합니다.
- 일정/업체현황 렌더링 중 `(f.ItemCycles || []).map is not a function` 오류를 차단합니다.
- item_cycles 객체에 일부 항목만 있더라도 facilities.items의 나머지 측정항목을 기본 주기로 보완합니다.


## v100 LAB 통합
- 시료채취 계산 RAW DATA 최종 배출량은 Sm3/min만 표시
- 메뉴명 LAB로 단순화
- NOx/SOx/CO 자동분석기 3회 평균 및 NOx/SOx 산소보정
- HCl, 플루오린, NH3, H2S, HCN, 브로민 계산식 추가
- 중금속 7종 공통식과 VOC 13종 통합 선택식 추가
- Excel 템플릿 calcChain 제거로 다운로드 후 복구 경고 방지

## v101 시료채취 불러오기 + LAB 가독성 보강
- Excel 불러오기 날짜: 접수번호가 아니라 기록지 우측 `측정날짜` 표시 셀(Q2)을 최우선으로 사용하고, Date timezone 하루 밀림 제거
- 웹 출력 XLSM에 전체 입력상태를 기록부 AR100(기존 사용 범위 안)에 백업하여 수분 1~5차/흡습관법/내경/홀더온도 등 재불러오기 안정화
- 자동수분 raw 값이 등속계산 탭에 있으면 평균 검증과 무관하게 1~5차를 그대로 복원
- 입자상 가스온도/홀더온도 입력 및 평균을 소수점 1자리 기준으로 보강
- 측정구 내경/굴뚝 형상은 기록지 G18 및 기록부 T8 fallback + 전체상태 백업으로 재복원
- 시료채취 계산 RAW DATA 최종 배출량에 Qa 유량과 Q 산소보정 유량을 모두 Sm³/min으로 함께 표시
- 먼지 LAB 카드에 표준산소농도보정 체크박스와 보정결과 추가
- 질소산화물/황산화물/일산화탄소 1·2·3차 값을 한 줄 가로배치, 농도/평균/보정 결과 소수점 첫째자리
- NOx/SOx 표준산소농도보정 식을 분자/분모 형태로 표시
- 먼지 및 전체 LAB 분석항목 제목 글자 확대, 모든 계산카드를 먼지 카드와 동일한 제목→계산식→기호표→입력→결과 틀로 통일
- XLSM 템플릿 내 calcChain 없는 버전을 다시 내장해 fetch fallback에서도 복구경고 재발 방지

## v102 LAB/불러오기 최종 보강
- Excel 불러오기 측정날짜는 기록지(먼지) Q2의 표시문자보다 원본 Excel serial 값을 우선하여 하루 전으로 밀리는 현상을 차단
- 원형 내경과 홀더온도는 raw cell 값 우선 복원으로 보강
- 먼지 여지무게 입력 순서를 채취 후 → 채취 전으로 변경
- LAB에 접수번호별 `LAB 저장` 버튼과 저장 상태 표시 추가(기존 자동저장도 유지)
- 시료채취기록에서 넘어오는 Vs 등 연동값은 LAB에서 readonly 처리
- 중금속/VOC/습식분석 계산식을 분수형 표기로 보강하고 입력값 추적 표시 추가
- 먼지/질소산화물/황산화물 등 산소보정 항목은 별도 산소보정 결과칸을 없애고, 체크 여부에 따라 최종결과 한 칸에 원값 또는 산소보정값만 표시
- NOx/SOx/CO는 1·2·3차 및 최종결과 소수점 첫째자리 유지

## v103 (2026-09-01) — LAB 시료채취 연동 분리
- LAB 상단 분석대상/실험항목 표시 제거. 실제 시료채취기록에 등록된 항목으로 카드 자동 생성.
- 중금속 시료채취 탭에 `중금속 항목` 선택 추가(구리/크로뮴/니켈/아연/납/베릴륨/카드뮴).
- LAB 중금속 카드 제목을 `중금속 · 세부항목`, VOC 카드를 `휘발성유기화합물 · 세부항목`으로 표시하고 LAB 내 항목 선택 드롭다운 제거.
- 가스상 분석의 Vs는 먼지 채취량을 사용하지 않고 가스시료채취 행의 채취량·가스미터온도·가스미터압과 현장 대기압으로 표준상태 건조가스량을 자동 환산.
  - 분석자료(2) 기준: Vs = V/(273+Tm) × 273/760 × (Pa+Pm)
- 중금속 Vs는 반드시 중금속 시료채취기록 자체의 건식가스미터 채취량/온도/압력차를 표준상태로 환산하여 사용.
- 중금속 분석용액 전체부피 V는 기존 양식 기준 250 mL로 잠금. 시료채취 연동값은 LAB에서 수정 불가.
- LAB 입력란/제목/읽기전용 표시 크기와 가독성 보강.

## v105 LAB 중금속/표시 보강
- LAB의 입력영역 글자, 단위, 안내문, 입력칸 포인트를 먼지 계산카드 수준으로 축소/통일했습니다.
- 중금속 시료채취기록에서 구리·크로뮴·니켈·아연·납·베릴륨·카드뮴을 선택하면 선택 항목별 LAB 계산카드가 생성됩니다.
- 각 중금속 카드의 식과 입력항목명에 실제 선택된 금속명을 표시하고, a/b/V/Vs 입력 시 농도 C=(a-b)×V/Vs 를 mg/Sm³로 계산합니다.
- 대입값 추적줄에도 선택 중금속명을 함께 표시합니다.

## v106 — 먼지/중금속 공통정보 공유 + 빈 기본화면
- 기본 시료채취 화면의 예시 업체/시설/접수번호/담당자/기상/O2/CO2/수분/내경/피토관/입자상 예시값을 제거했습니다.
- 홀더온도만 실무 기본값 120.0 ℃로 자동 적용됩니다. Excel 불러오기에서 홀더온도가 비어 있어도 120 ℃를 사용합니다.
- 먼지와 중금속은 최초 탭 전환 시 업체·시설·내경·O2·CO2·수분·기상·장비·입자상 측정조건을 한 번 복사하여 같은 현장조건에서 시작합니다.
- 최초 공유 이후 먼지/중금속 입자상 측정조건은 각 탭에서 독립 수정할 수 있습니다.
- 접수번호·여지번호는 먼지/중금속 사이에 공유하지 않습니다.


## v107
- 시료채취 유형에 `먼지 + 중금속` 추가. 기본정보/기상/O2/CO2/수분은 1회 입력, 먼지/중금속 입자상 측정조건은 내부 탭으로 각각 독립 저장.
- 복합기록은 하나의 접수번호를 공유할 수 있고 LAB에서 먼지와 선택 중금속을 함께 생성.
- 중금속 Vs는 복합기록의 중금속 입자상 측정조건에서 별도 환산. 먼지 농도는 먼지 입자상 조건 사용.
- VOC/염화수소/플루오린/암모니아/황화수소 등 LAB 카드는 동일한 먼지형 레이아웃 유지.
- 이황화탄소/페놀은 첨부 원본에 분석식이 없어 임의 산식을 넣지 않고 카드/시료채취 연동만 준비.


## v108 — 피토관계수 + Excel 측정점 그림 보강
- 먼지 / 중금속 / 먼지+중금속 모든 시료채취 유형의 피토관계수 기본값을 0.83으로 복원.
- 과거 저장본 또는 Excel 불러오기에서 피토관계수가 비어 있어도 0.83으로 자동 보강.
- Excel 추출 직전에 현재 측정점 SVG를 다시 계산/렌더링한 뒤 PNG로 삽입.
- 측정점 그림의 위치와 크기는 기존 확정 샘플 좌표(col 9, row 8, cx 3132000, cy 2395122)를 그대로 유지.
- 먼지, 중금속, 먼지+중금속 모두 동일한 그림 삽입 로직 적용.

## v108.1 측정점 그림 핫픽스
- Excel XLSM 패키지에 `xl/media/traverse_generated.png`를 추가하면서 `[Content_Types].xml`에 PNG MIME 선언이 누락되어 Excel이 그림을 표시하지 않던 문제 수정.
- 측정점 PNG 생성 실패 시 조용히 넘어가지 않고 Excel 출력 오류로 알려 원인 확인 가능하게 변경.
- 기존에 확정한 측정점 그림 위치/크기 앵커값은 그대로 유지.

## v108.2 Excel 측정점 그림 실제 삽입 수정
- 원본 XLSM의 `xl/drawings/_rels/drawing1.xml.rels`가 존재하지 않는 것을 확인했습니다.
- v108/v108.1은 이 관계 파일이 없으면 그림 삽입 함수를 중단(return)하여 PNG가 실제 Excel에 들어가지 않았습니다.
- v108.2는 관계 파일이 없으면 직접 생성하고, `xl/media/traverse_generated.png`를 drawing1에 연결합니다.
- 기존에 맞춘 측정점 이미지 위치/크기 앵커값은 그대로 유지합니다.
